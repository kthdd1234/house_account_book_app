enum SvgDirectionEnum { left, right }

enum JumpDayTypeEnum { none, subtract, add }

enum SegmentedTypeEnum {
  income,
  spend,
  week,
  twoWeek,
  month,
  threeMonth,
  sixMonth,
  oneYear,
  custom,
}

enum HouseholdLedgerEnum { income, spend }
